Could you help me create a new cypress test, consider the following:

- Use the file inside the `chrome-recordings` folder as a base of the user flow that needs to be a cypress test.
- If there's more than one file, ask the user to select the one that they want to use as a base.
- For each file analyze the browser actions, network requests, error messages and console logs
- Always be backwards compatible with any of your changes.
- Follow all the guidelines in [AGENTS.md](../AGENTS.md)
- Everytime you have a test ready, please run and debug it using the steps in [ai-debugging.md](ai-debugging.md) (`prompts/ai-debugging.md`)
